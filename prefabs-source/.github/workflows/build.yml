name: build

on:
  push:
    branches: [ "master" ]
    tags:
      - "v*.*.*"
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Cache
        uses: actions/cache@v2
        with:
          path: Library
          key: Library-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-

      - name: Restore dotnet packages
        run: dotnet restore "World Prefabs.sln"

      - name: Restore NuGet packages
        run: nuget restore "World Prefabs.sln"

      - name: Create Unity build
        uses: game-ci/unity-builder@v2.0.4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

      - name: Copy files to destination
        shell: bash
        run: |
          set -e
          set +x

          mkdir -p ./dll-build/UserLibs
          DIR=build/StandaloneWindows64/StandaloneWindows64_Data

          cp -a $DIR/Plugins/x86_64/* $DIR/Managed/LibVLCSharp.dll $DIR/Managed/DecentM.*.dll ./dll-build/UserLibs

      - name: Create zip
        uses: vimtor/action-zip@v1
        with:
          files: dll-build/
          dest: DecentM.Components.zip

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: DecentM.Components.zip

      - name: Upload Unity build
        uses: actions/upload-artifact@v2
        with:
          name: UnityBuild
          path: build

      - name: Upload built DLLs
        uses: actions/upload-artifact@v2
        with:
          name: DLLs
          path: dll-build

      - name: Trigger Mods build workflow
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          set +x

          gh workflow run build.yml -R DecentM/cvr-mods -f prefabs_action=$GITHUB_ACTION -f artifact_name=DLLs
          # REPO=DecentM/cvr-mods
          # curl -XPOST -u "${{ secrets.PAT_USERNAME }}:${{ secrets.PAT_TOKEN }}" -H "Accept: application/vnd.github.everest-preview+json" -H "Content-Type: application/json" https://api.github.com/repos/$REPO/dispatches --data '{"prefabs_action": "$GITHUB_ACTION", "artifact_name": "DLLs"}'
